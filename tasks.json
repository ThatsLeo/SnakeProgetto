{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Compila automaticamente",
            "type": "shell",
            "command": "g++ -I/mingw64/include/ncurses -o NOME_FILE.exe NOME_FILE.cpp -lncurses -L/mingw64/bin -static",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}
